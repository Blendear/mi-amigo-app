/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";
import { ChapterOfInfoProps } from "../types";

const ChapterOfInfo = ({}: ChapterOfInfoProps) => {
  return <div>ChapterOfInfo</div>;
};

export default ChapterOfInfo;
